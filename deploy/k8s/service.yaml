apiVersion: v1
kind: Service
metadata:
  name: carbonorm-docs
  namespace: carbonorm
  labels:
    app.kubernetes.io/name: carbonorm-docs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: carbonorm-docs
  ports:
    - name: http
      port: 80
      targetPort: http
