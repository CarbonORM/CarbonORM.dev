apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: carbonorm-docs-miles-systems-tls
  namespace: carbonorm
spec:
  secretName: carbonorm-docs-miles-systems-tls
  dnsNames:
    - carbonorm.miles.systems
  issuerRef:
    name: letsencrypt-miles-systems-dns-prod
    kind: ClusterIssuer
